# Files taken from the mlx-optiq package (MIT); see ../../../VENDORED.md.
# Ported to the vendored mlx-lm cache API and stripped of their monkeypatch
# installers: the engine wires them in explicitly.

UPSTREAM_VERSION = "0.5.6"
