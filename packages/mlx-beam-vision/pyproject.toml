[build-system]
requires = ["hatchling>=1.27", "hatch-vcs>=0.4"]
build-backend = "hatchling.build"

[project]
name = "mlx-beam-vision"
dynamic = ["version"]
description = "Vision towers for mlx-beam: images in the prompt, served by the checkpoint's own tower."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.12"
authors = [{ name = "the mlx-beam contributors" }]
keywords = ["mlx", "apple-silicon", "vlm", "vision", "inference"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: MacOS",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "mlx-beam",
  "mlx>=0.32; sys_platform == 'darwin'",
  # Muse Glimmer's processor arrived in 5.15; Gemma 4 and Granite Vision
  # 4.1 are older.
  "transformers>=5.15,<6",
  # The processors transformers ships for these families are written on
  # torch (Gemma 4 imports it at module level) and torchvision (the Qwen
  # and Muse processors build a video processor on it); the towers run on
  # MLX. Measured 2026-09-25: +260 MB RSS and +0.5 s per process.
  "torch>=2.5,<3",
  "torchvision",
  "pillow>=10",
  "numpy",
]

[project.optional-dependencies]
dev = ["pytest>=8", "mlx[cpu]>=0.32; sys_platform == 'linux'"]

# In the workspace the core is the checkout beside this package, not
# PyPI's; the wheel's dependency stays the plain name.
[tool.uv.sources]
mlx-beam = { workspace = true }

# What mlx-beam looks for: one provider, dispatching by model type.
[project.entry-points."mlx_beam.modalities"]
vision = "mlx_beam_vision.provider"

[project.urls]
Homepage = "https://p4ik.github.io/mlx-beam/"
Repository = "https://github.com/p4ik/mlx-beam"

[tool.hatch.version]
source = "vcs"
raw-options = { root = "../..", local_scheme = "no-local-version", version_scheme = "guess-next-dev" }

[tool.hatch.build.hooks.vcs]
version-file = "mlx_beam_vision/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["mlx_beam_vision"]

[tool.hatch.build.targets.sdist]
only-include = ["mlx_beam_vision", "tests", "README.md", "LICENSE", "NOTICE"]

[tool.pytest.ini_options]
testpaths = ["tests"]
